services:
  postgres:
    image: postgres:16-alpine
    container_name: next-learning-postgres
    environment:
      POSTGRES_DB: next_learning
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d next_learning"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
